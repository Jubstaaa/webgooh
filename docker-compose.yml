services:
    web:
        image: ghcr.io/jubstaaa/webgooh:latest
        container_name: webgooh
        restart: unless-stopped
        expose:
            - '3000'
        environment:
            NODE_ENV: production
            NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL}
            PAYLOAD_SECRET: ${PAYLOAD_SECRET}
            DATABASE_URI: file:/app/data/webgooh.db
            REVALIDATION_SECRET: ${REVALIDATION_SECRET}
            PREVIEW_SECRET: ${PREVIEW_SECRET}
            TURNSTILE_SECRET_KEY: ${TURNSTILE_SECRET_KEY}
        volumes:
            - /opt/webgooh/data:/app/data
            - /opt/webgooh/media:/app/media

    caddy:
        image: caddy:2-alpine
        container_name: webgooh-caddy
        restart: unless-stopped
        depends_on:
            - web
        ports:
            - '80:80'
            - '443:443'
        volumes:
            - /opt/webgooh/Caddyfile:/etc/caddy/Caddyfile:ro
            - /opt/webgooh/certs:/etc/caddy/certs:ro
            - caddy-data:/data
            - caddy-config:/config

volumes:
    caddy-data:
    caddy-config:
